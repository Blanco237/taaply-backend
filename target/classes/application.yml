spring:
  application:
    name: todo

  # PostgreSQL Database Configuration
  datasource:
    # Neon connection URL with SSL enabled
    url: jdbc:postgresql://ep-steep-bird-ag3m07kh-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_1Fk6McLZgOBb
    driver-class-name: org.postgresql.Driver

  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: validate # Use Flyway for schema management
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Flyway Database Migration
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    # Flyway user needs to be the same as datasource usually
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}

# Server Configuration
server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

# JWT Configuration
jwt:
  secret: your-256-bit-secret-key-here-change-in-production-min-32-chars
  expiration: 86400000 # 24 hours in milliseconds

# Logging Configuration
logging:
  level:
    root: INFO
    com.taaply.todo: DEBUG
    org.springframework.security: DEBUG
